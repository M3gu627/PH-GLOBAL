name: ph_global
description: "PH Global Appointment Notifier"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.4.3
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.0
  supabase_flutter: ^2.15.0
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/applogo.png